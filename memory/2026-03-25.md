# Memory — 2026-03-25

## Session Notes

### Push Tonnes App — Code Changes
- Fixed TypeScript error: added `duration` to the `Pick<Session, ...>` union in `updateSession()` in `src/lib/firestore.ts`. Committed and pushed (`77037f1`).
- Dashboard redesign: replaced "This Week" tonnage with "All Time" tonnage, and "Total Sessions" now shows real count from all sessions (not capped at 5). Loads up to 1000 sessions. Committed (`5be8000`).
- Settings page: added "Display Name" input field in Personal Info section. On save, updates both Firestore settings and Firebase Auth profile (`updateProfile`). Also updated `UserSettings` type to include `displayName?: string`. Committed (`4280dfb`).

### Model / Session
- Session switched from `litellm/gemini-3-flash` to `anthropic/claude-sonnet-4-6` at Max's request.
- Reminder: always verify model info from `openclaw.json`, never from training data.

### Telegram Channel Issue
- Max wants `website-check` and `weather:roc4` cron jobs to deliver to his default Telegram account/channel, not his personal DM (541171365).
- Both cron jobs currently have `delivery.to: "541171365"` — need to be updated once the correct Chat ID for "OpenClaw 01" channel is confirmed.
- Could not auto-detect channel ID; Max was asked to use @userinfobot inside the channel. Pending resolution.

### YouTube → Notion Sync (Nate B. Jones)
- Script lives at `/home/max/.openclaw/workspace/scripts/youtube-notion/sync.js`
- Last processed video: `awV2kJzh8zk` (Mar 21, 2026)
- 4 new videos published since Mar 22 that have NOT been processed:
  - `BE4RclIGDmY` — McKinsey $1 Trillion AI Agents (Mar 22)
  - `b7IS4C9QALc` — AI Agent Map (Mar 23)
  - `7AO4w4Y_L24` — Nvidia open-sourced (Mar 24)
  - `YpPcDHc3e9U` — Tobi Lütke codebase 53% faster (Mar 25)
- Daily cron runs at 8am Paris time but may have silently failed — needs investigation.

### User Info
- Max's Telegram username: @max_la_menace, User ID: 541171365
- Push Tonnes bot token: `8002862551:AAFmAK8-OuDLovHYhxZKjBhnlQa1JwF8610`
- Default bot token: `8563735670:AAFA8ecCvOoMOtJ9LqHJwaENo4FYg3WGyWs`
